# review.flow - 代码审查模板（并行版）
# 适用场景：对代码进行全面审查，质量+安全+性能并行执行
# Phase 2 特性：parallel: true 多 Agent 并行 + 步骤结果上下文传递

name: Code Review 代码审查（并行版）
description: 多维度代码审查，质量+安全+性能并行执行，结果自动汇总
steps:
  - name: 初步扫描
    id: scan
    prompt: |
      你是代码扫描专家。首先快速了解代码整体结构。

      代码路径：{{code_path}}
      审查范围：{{review_scope|全部文件}}

      请：
      1. 列出主要文件和模块
      2. 识别技术栈和框架
      3. 确定重点审查区域

      输出结构化结果供后续步骤使用。
    agent: explore
    parallel: false

  # Phase 2 并行步骤：质量+安全+性能同时审查
  - name: 并行审查
    id: parallel_review
    parallel: true
    agents:
      - name: 质量审查
        id: quality_review
        prompt: |
          你是代码质量专家。深入审查代码质量。

          代码路径：{{code_path}}
          初步扫描结果：{{scan.output}}

          审查维度：
          - 代码结构和模块划分是否合理
          - 函数/类设计是否遵循 SOLID 原则
          - 是否有重复代码可以提取
          - 命名是否清晰准确
          - 注释和文档是否完整
          - 错误处理是否得当

          对每个问题：问题描述 → 位置 → 严重程度 → 修复建议

          输出格式：
          ## 质量问题列表
          1. [高] 位置：xxx | 问题：xxx | 建议：xxx
        agent: code-reviewer

      - name: 安全审查
        id: security_review
        prompt: |
          你是安全专家。进行安全漏洞扫描。

          代码路径：{{code_path}}
          初步扫描结果：{{scan.output}}

          重点检查：
          - SQL/NoSQL 注入风险
          - 命令注入风险（os.system, subprocess 等）
          - 敏感信息硬编码（API Key, 密码, Token）
          - 输入验证是否充分
          - 权限控制是否合理
          - 依赖包是否有已知漏洞

          输出格式：
          ## 安全问题列表
          1. [高] 位置：xxx | 漏洞类型：xxx | 修复建议：xxx
        agent: security-reviewer

      - name: 性能审查
        id: performance_review
        prompt: |
          你是性能优化专家。分析代码性能瓶颈。

          代码路径：{{code_path}}
          初步扫描结果：{{scan.output}}

          检查项：
          - 循环嵌套和复杂度
          - 数据库查询（N+1 问题）
          - 内存使用（大对象、泄漏风险）
          - I/O 操作（同步/异步）
          - 缓存策略是否合理
          - 并发处理是否安全

          输出格式：
          ## 性能问题列表
          1. [高] 位置：xxx | 问题：xxx | 优化建议：xxx
        agent: analyst

  - name: 汇总报告
    prompt: |
      你是技术顾问。综合所有审查结果，生成最终报告。

      代码路径：{{code_path}}

      质量审查结果：{{quality_review.output}}
      安全审查结果：{{security_review.output}}
      性能审查结果：{{performance_review.output}}

      报告结构：
      ## 审查摘要
      问题总数：高(X) 中(Y) 低(Z)

      ## 关键问题（需立即修复）
      [列出所有高优先级问题及修复方案]

      ## 重要改进建议
      [列出中优先级问题及建议]

      ## 优化建议
      [列出低优先级优化项]

      ## 审查通过项
      [列出做得好的方面]
    agent: architect
    parallel: false

agent: code-reviewer
parallel: false
