# 广发证券：125个经典技术指标择时分析

> 来源：广发证券金融工程研究报告  
> 日期：2020年1月3日  
> 回测区间：2005-04-08 至 2019-12-23（沪深300指数）

---

## 一、基础函数

| 函数 | 定义 |
|------|------|
| `REF(X,N)` | N天前X的值 |
| `IF(COND,A,B)` | 条件选择 |
| `SUM(X,N)` | 过去N天求和 |
| `CUMSUM(X)` | 累积和 |
| `MAX(X,N)` | 过去N天最大值 |
| `MIN(X,N)` | 过去N天最小值 |
| `ABS(X)` | 绝对值 |

---

## 二、移动平均

| 指标 | 公式 |
|------|------|
| **MA** | `MA(X,N) = SUM(X,N)/N` |
| **EMA** | `EMA = 2/(N+1)*X + (N-1)/(N+1)*REF(EMA,1)` |
| **SMA** | `SMA = M/N*X + (N-M)/N*REF(SMA,1)` |
| **WMA** | 加权移动平均 |
| **DMA** | `DMA = a*X + (1-a)*REF(X,1)` |

---

## 三、价格动量指标

### DPO (去势价格摆动)
```
DPO = CLOSE - REF(MA(CLOSE,N), N/2+1)
```
- DPO>0 多头市场，DPO<0 空头市场
- **买入**：DPO上穿0
- **卖出**：DPO下穿0

### ER (动力指标)
```
BullPower = HIGH - EMA(CLOSE,N)
BearPower = LOW - EMA(CLOSE,N)
```
- BullPower>0且BearPower>0 多头主导
- **买入**：BearPower上穿0
- **卖出**：BullPower下穿0

### TII (趋势指标)
```
CLOSE_MA = MA(CLOSE,N1)
DEV = CLOSE - CLOSE_MA
DEVPOS = IF(DEV>0, DEV, 0)
DEVNEG = IF(DEV<0, -DEV, 0)
SUMPOS = SUM(DEVPOS, M)
SUMNEG = SUM(DEVNEG, M)
TII = 100 * SUMPOS / (SUMPOS + SUMNEG)
TII_SIGNAL = EMA(TII, N2)
```
- **买入**：TII上穿TII_SIGNAL
- **卖出**：TII下穿TII_SIGNAL

### PO (价格摆动)
```
EMA_SHORT = EMA(CLOSE,9)
EMA_LONG = EMA(CLOSE,26)
PO = (EMA_SHORT - EMA_LONG) / EMA_LONG * 100
```
- **买入**：PO上穿0
- **卖出**：PO下穿0

### MADisplaced
```
MA_CLOSE = MA(CLOSE,N)
MADisplaced = REF(MA_CLOSE,M)
```
- **买入**：收盘价上穿MADisplaced
- **卖出**：收盘价下穿MADisplaced

### T3 (三重指数移动平均)
```
T1 = EMA(CLOSE,N)*(1+VA) - EMA(EMA(CLOSE,N),N)*VA
T2 = EMA(T1,N)*(1+VA) - EMA(EMA(T1,N),N)*VA
T3 = EMA(T2,N)*(1+VA) - EMA(EMA(T2,N),N)*VA
```

### POS (位置指标)
```
PRICE = (CLOSE - REF(CLOSE,N)) / REF(CLOSE,N)
POS = (PRICE - MIN(PRICE,N)) / (MAX(PRICE,N) - MIN(PRICE,N))
```
- **买入**：POS上穿80
- **卖出**：POS下穿20

### PAC (价格通道)
```
UPPER = SMA(HIGH,N1,1)
LOWER = SMA(LOW,N2,1)
```
- **买入**：价格突破上轨
- **卖出**：价格突破下轨

### ADTM (动态人气指标)
```
DTM = IF(OPEN>REF(OPEN,1), MAX(HIGH-OPEN, OPEN-REF(OPEN,1)), 0)
DBM = IF(OPEN<REF(OPEN,1), MAX(OPEN-LOW, REF(OPEN,1)-OPEN), 0)
STM = SUM(DTM,N)
SBM = SUM(DBM,N)
ADTM = (STM - SBM) / MAX(STM, SBM)
```
- **买入**：ADTM上穿0.5
- **卖出**：ADTM下穿-0.5

### ZLMACD (零滞后MACD)
```
ZLMACD = (2*EMA(CLOSE,N1) - EMA(EMA(CLOSE,N1),N1)) - (2*EMA(CLOSE,N2) - EMA(EMA(CLOSE,N2),N2))
```
- **买入**：ZLMACD上穿0
- **卖出**：ZLMACD下穿0

### TMA (三角移动平均)
```
TMA = MA(MA(CLOSE,N), N)
```

### TYP (典型价格)
```
TYP = (CLOSE + HIGH + LOW) / 3
TYPMA1 = EMA(TYP,N1)
TYPMA2 = EMA(TYP,N2)
```
- **买入**：TYPMA1上穿TYPMA2

### KDJD
```
LOW_N = MIN(LOW,N)
HIGH_N = MAX(HIGH,N)
Stochastics = (CLOSE - LOW_N) / (HIGH_N - LOW_N) * 100
K = SMA(Stochastics,3,1)
D = SMA(K,3,1)
```
- **买入**：D上穿70
- **卖出**：D下穿30

### VMA
```
PRICE = (HIGH + LOW + OPEN + CLOSE) / 4
VMA = MA(PRICE,N)
```

### BIAS (乖离率)
```
BIAS(N) = (CLOSE - MA(CLOSE,N)) / MA(CLOSE,N) * 100
```
- **买入**：BIAS6>5 且 BIAS12>7 且 BIAS24>11
- **卖出**：BIAS6<-5 且 BIAS12<-7 且 BIAS24<-11

### WMA (加权移动平均)
```
WMA = (N*CLOSE + (N-1)*REF(CLOSE,1) + ... + 1*REF(CLOSE,N-1)) / (1+2+...+N)
```

### DDI
```
HL = HIGH + LOW
HIGH_ABS = ABS(HIGH - REF(HIGH,1))
LOW_ABS = ABS(LOW - REF(LOW,1))
DMZ = IF(HL > REF(HL,1), MAX(HIGH_ABS, LOW_ABS), 0)
DMF = IF(HL < REF(HL,1), MAX(HIGH_ABS, LOW_ABS), 0)
DIZ = SUM(DMZ,N) / (SUM(DMZ,N) + SUM(DMF,N))
DIF = SUM(DMF,N) / (SUM(DMZ,N) + SUM(DMF,N))
DDI = DIZ - DIF
```
- **买入**：DDI上穿0
- **卖出**：DDI下穿0

### SROC
```
EMAP = EMA(CLOSE,N)
SROC = (EMAP - REF(EMAP,M)) / REF(EMAP,M)
```

### DC (唐奇安通道)
```
UPPER = MAX(HIGH,N)
LOWER = MIN(LOW,N)
MIDDLE = (UPPER + LOWER) / 2
```

### VIDYA
```
VI = ABS(CLOSE - REF(CLOSE,N)) / SUM(ABS(CLOSE - REF(CLOSE,1)), N)
VIDYA = VI * CLOSE + (1 - VI) * REF(CLOSE,1)
```

### Qstick
```
Qstick = MA(CLOSE - OPEN, N)
```
- **买入**：Qstick上穿0

### FB (斐波那契带)
```
TR = MAX(HIGH-LOW, ABS(HIGH-REF(CLOSE,1)), ABS(LOW-REF(CLOSE,1)))
ATR = MA(TR,N)
UPPER = MA(CLOSE,N) + k*ATR
LOWER = MA(CLOSE,N) - k*ATR
```

### DEMA
```
DEMA = 2*EMA(CLOSE,N) - EMA(EMA(CLOSE,N),N)
```

### APZ
```
VOL = EMA(EMA(HIGH-LOW,N),N)
UPPER = EMA(EMA(CLOSE,M),M) + PARAM*VOL
LOWER = EMA(EMA(CLOSE,M),M) - PARAM*VOL
```

### ASI
```
SI = 50 * (CLOSE - REF(CLOSE,1) + (REF(CLOSE,1) - REF(OPEN,1)) + 0.5*(CLOSE-OPEN)) / R * K / M
ASI = CUMSUM(SI)
```

### Arron
```
ArronUp = (N - HIGH_LEN) / N * 100
ArronDown = (N - LOW_LEN) / N * 100
ArronOs = ArronUp - ArronDown
```
- **买入**：ArronOs上穿20
- **卖出**：ArronOs下穿-20

### KC (肯特纳通道)
```
TR = MAX(ABS(HIGH-LOW), ABS(HIGH-REF(CLOSE,1)), ABS(REF(CLOSE,1)-REF(LOW,1)))
ATR = MA(TR,N)
UPPER = EMA(CLOSE,20) + 2*ATR
LOWER = EMA(CLOSE,20) - 2*ATR
```

### MTM
```
MTM = CLOSE - REF(CLOSE,N)
```

### CR
```
TYP = (HIGH + LOW + CLOSE) / 3
H = MAX(HIGH - REF(TYP,1), 0)
L = MAX(REF(TYP,1) - LOW, 0)
CR = SUM(H,N) / SUM(L,N) * 100
```
- **买入**：CR上穿200
- **卖出**：CR下穿50

### BOP
```
BOP = MA((CLOSE-OPEN)/(HIGH-LOW), N)
```
- **买入**：BOP上穿0

### HULLMA
```
X = 2*EMA(CLOSE, [N/2]) - EMA(CLOSE,N)
HULLMA = EMA(X, √N)
```

### COPP
```
RC = 100 * ((CLOSE-REF(CLOSE,N1))/REF(CLOSE,N1) + (CLOSE-REF(CLOSE,N2))/REF(CLOSE,N2))
COPP = WMA(RC,M)
```

### ENV (包络线)
```
UPPER = MA(CLOSE,N) * (1 + PARAM)
LOWER = MA(CLOSE,N) * (1 - PARAM)
```

### RSIH
```
RSI = SMA(MAX(CLOSE-REF(CLOSE,1),0),N,1) / SMA(ABS(CLOSE-REF(CLOSE,1)),N,1) * 100
RSI_SIGNAL = EMA(RSI,N2)
RSIH = RSI - RSI_SIGNAL
```

### BBI
```
BBI = (MA(CLOSE,3) + MA(CLOSE,6) + MA(CLOSE,12) + MA(CLOSE,24)) / 4
```

### TRIX
```
TRIX = (EMA(EMA(EMA(CLOSE,N),N),N) - REF(EMA(EMA(EMA(CLOSE,N),N),N),1)) / REF(EMA(EMA(EMA(CLOSE,N),N),N),1)
```
- **买入**：TRIX上穿0

### ADX
```
+DM = IF(HIGH>REF(HIGH,1), HIGH-REF(HIGH,1), 0)
-DM = IF(LOW<REF(LOW,1), REF(LOW,1)-LOW, 0)
DI+ = SUM(+DM,N) / SUM(TR,N) * 100
DI- = SUM(-DM,N) / SUM(TR,N) * 100
```
- **买入**：DI+上穿DI-

### FISHER
```
PRICE_CH = 2 * (PRICE - MIN(LOW,N)/(MAX(HIGH,N)-MIN(LOW,N)) - 0.5)
FISHER = 0.5*REF(FISHER,1) + 0.5*log((1+PRICE_CHANGE)/(1-PRICE_CHANGE))
```

### TSI
```
TSI = EMA(EMA(CLOSE-REF(CLOSE,1),N1),N2) / EMA(EMA(ABS(CLOSE-REF(CLOSE,1)),N1),N2) * 100
```
- **买入**：TSI上穿10
- **卖出**：TSI下穿-10

### CMO
```
SU = SUM(MAX(CLOSE-REF(CLOSE,1),0),N)
SD = SUM(MAX(REF(CLOSE,1)-CLOSE,0),N)
CMO = (SU - SD) / (SU + SD) * 100
```
- **买入**：CMO上穿30

### OSC
```
OSC = CLOSE - MA(CLOSE,N)
OSCMA = MA(OSC,M)
```

### MACD
```
MACD = EMA(CLOSE,N1) - EMA(CLOSE,N2)
MACD_SIGNAL = EMA(MACD,N3)
```
- **买入**：MACD上穿0

### CLV
```
CLV = (2*CLOSE - LOW - HIGH) / (HIGH - LOW)
```
- CLV>0 收盘价更靠近最高价

### WAD
```
TRH = MAX(HIGH, REF(CLOSE,1))
TRL = MIN(LOW, REF(CLOSE,1))
AD = IF(CLOSE>REF(CLOSE,1), CLOSE-TRL, IF(CLOSE<REF(CLOSE,1), CLOSE-TRH, 0))
WAD = CUMSUM(AD)
```

### KAMA
```
ER = (CLOSE - REF(CLOSE,N)) / SUM(ABS(CLOSE-REF(CLOSE,1)), N)
SMOOTH = ER*(2/(N1+1) - 2/(N2+1)) + 2/(N2+1)
COF = SMOOTH * SMOOTH
KAMA = COF*CLOSE + (1-COF)*REF(KAMA,1)
```

### PPO
```
PPO = (EMA(CLOSE,N1) - EMA(CLOSE,N2)) / EMA(CLOSE,N2)
```
- **买入**：PPO上穿信号线

---

## 四、价格反转指标

### KDJ
```
RSV = (CLOSE - MIN(LOW,N)) / (MAX(HIGH,N) - MIN(LOW,N)) * 100
K = SMA(RSV,3,1)
D = SMA(K,3,1)
```
- **买入**：D<20 且 K上穿D
- **卖出**：D>80 且 K下穿D

### RSI
```
CLOSEUP = IF(CLOSE>REF(CLOSE,1), CLOSE-REF(CLOSE,1), 0)
CLOSEDOWN = IF(CLOSE<REF(CLOSE,1), ABS(CLOSE-REF(CLOSE,1)), 0)
RSI = 100 * SMA(CLOSEUP,N,1) / (SMA(CLOSEUP,N,1) + SMA(CLOSEDOWN,N,1))
```
- **买入**：RSI上穿40
- **卖出**：RSI下穿60

### CCI
```
TP = (HIGH + LOW + CLOSE) / 3
CCI = (TP - MA(TP,N)) / (0.015 * MA(ABS(TP-MA(TP,N)),N))
```
- **买入**：CCI上穿-100
- **卖出**：CCI下穿100

### WR
```
WR = 100 * (MAX(HIGH,N) - CLOSE) / (MAX(HIGH,N) - MIN(LOW,N))
```
- **买入**：WR上穿80
- **卖出**：WR下穿20

### ROC
```
ROC = (CLOSE - REF(CLOSE,100)) / REF(CLOSE,100) * 100
```
- **买入**：ROC上穿5%
- **卖出**：ROC下穿-5%

### STC
```
MACDX = EMA(CLOSE,N1) - EMA(CLOSE,N2)
FK = (MACDX - MIN(MACDX,N)) / (MAX(MACDX,N) - MIN(MACDX,N)) * 100
FD = SMA(FK,N,1)
SK = (FD - MIN(FD,N)) / (MAX(FD,N) - MIN(FD,N)) * 100
STC = SMA(SK,N,1)
```
- **买入**：STC上穿25
- **卖出**：STC下穿75

---

## 五、成交量指标

### MAAMT
```
MAAMT = MA(AMOUNT,N)
```

### SROCVOL
```
EMAP = EMA(VOLUME,N)
SROCVOL = (EMAP - REF(EMAP,M)) / REF(EMAP,M)
```

### PVO
```
PVO = (EMA(VOLUME,N1) - EMA(VOLUME,N2)) / EMA(VOLUME,N2) * 100
```
- **买入**：PVO上穿0

### BIASVOL
```
BIASVOL(N) = (VOLUME - MA(VOLUME,N)) / MA(VOLUME,N)
```

### MACDVOL
```
MACDVOL = EMA(VOLUME,N1) - EMA(VOLUME,N2)
SIGNAL = MA(MACDVOL,N3)
```

### ROCVOL
```
ROCVOL = (VOLUME - REF(VOLUME,N)) / REF(VOLUME,N)
```

---

## 六、价量指标

### VWAP
```
Typical = (HIGH + LOW + CLOSE) / 3
MF = VOLUME * Typical
VWAP = SUM(MF,N) / SUM(VOLUME,N)
```
- **买入**：价格上穿VWAP

### FI ( force index)
```
FI = (CLOSE - REF(CLOSE,1)) * VOLUME
FIMA = EMA(FI,N)
```
- **买入**：FIMA上穿0

### NVI
```
NVI_INC = IF(VOLUME<REF(VOLUME,1), 1+(CLOSE-REF(CLOSE,1))/CLOSE, 1)
NVI = CUM_PROD(NVI_INC)
```

### PVT
```
PVT = (CLOSE - REF(CLOSE,1)) / REF(CLOSE,1) * VOLUME
```

### RSIV
```
VOLUP = IF(CLOSE>REF(CLOSE,1), VOLUME, 0)
VOLDOWN = IF(CLOSE<REF(CLOSE,1), VOLUME, 0)
RSIV = 100 * SUM(VOLUP,N) / (SUM(VOLUP,N) + SUM(VOLDOWN,N))
```

### OBV
```
OBV = REF(OBV,1) + IF(CLOSE>REF(CLOSE,1), VOLUME, IF(CLOSE<REF(CLOSE,1), -VOLUME, 0))
```

### CMF
```
CMF = SUM(((CLOSE-LOW)-(HIGH-CLOSE))*VOLUME/(HIGH-LOW),N) / SUM(VOLUME,N)
```
- **买入**：CMF上穿0

### MFI
```
MF = TYPICAL_PRICE * VOLUME
MF_POS = SUM(IF(TYPICAL_PRICE>=REF(TYPICAL_PRICE,1), MF, 0), N)
MF_NEG = SUM(IF(TYPICAL_PRICE<=REF(TYPICAL_PRICE,1), MF, 0), N)
MFI = 100 - 100/(1 + MF_POS/MF_NEG)
```
- **买入**：MFI上穿80

### VR
```
AV = IF(CLOSE>REF(CLOSE,1), VOLUME, 0)
BV = IF(CLOSE<REF(CLOSE,1), VOLUME, 0)
CV = IF(CLOSE=REF(CLOSE,1), VOLUME, 0)
VR = (SUM(AV,N) + SUM(CV,N)/2) / (SUM(BV,N) + SUM(CV,N)/2)
```
- **买入**：VR上穿250

---

## 七、大盘指标

### STIX
```
STIX = EMA(UP_STOCK/(UP_STOCK+DOWN_STOCK)*100, N)
```
- **买入**：STIX上穿50

### MIO
```
MIO = EMA((UP_STOCK-DOWN_STOCK)/(UP_STOCK+DOWN_STOCK),N1) - EMA((UP_STOCK-DOWN_STOCK)/(UP_STOCK+DOWN_STOCK),N2)
```

### ADIO
```
ADIO = MA(UP_STOCK - DOWN_STOCK, N)
```

### MCL
```
MCL = EMA(UP_STOCK-DOWN_STOCK,N1) - EMA(UP_STOCK-DOWN_STOCK,N2)
```
- **买入**：MCL上穿0

### ADR
```
ADR = MA(UP_STOCK/DOWN_STOCK, N)
```
- **买入**：ADR上穿1

---

*整理自广发证券《125个经典技术指标择时分析》研报*
