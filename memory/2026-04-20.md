# 2026-04-20 Daily Memory

## ai_coder 改造教训

### 背景
今天花了大半天折腾ai_coder的screen模式改造，最后发现完全没必要。

### 核心发现
**直接用 screen + codex/claude 就行了，不需要 ai_coder 封装层。**

```bash
# 本地Codex
screen -dmS codex-task bash -c "codex exec '任务' 2>&1 | tee /tmp/output.log"

# 韩国Codex
ssh -i ~/.ssh/id_ed25519 root@43.108.18.71 \
  "su - ccuser -c 'screen -dmS kr-task bash -c \"codex exec ...\"'"

# 本地Claude
screen -dmS claude-task bash -c "claude --print '任务'"
```

### 今天的错误
1. **过度工程**：明明screen+codex能直接跑，非要搞ai_coder封装层
2. **Codex分析过度**：Codex分析了一堆，但真正能用的修改不多
3. **我手动重写了代码**：Codex写了一套，我又自己改了一套，结果改坏了
4. **错误理解Reconnecting**：Codex的5次Reconnecting是正常初始化（读技能/配置/上下文），不是失败

### ai_coder现状
- 备份版本在 `/Volumes/cu/ocu/ai_coder_backup_20260420/`
- 当前版本：Codex的screen改造 + remote.py exec命令修复
- 但实际上**直接用screen调用更简单**

### 重要规则（雪子强调）
**以后改任何代码之前必须先问雪子，经过同意再动。**

### 统一调用方式：screen模式
**核心发现**：所有工具调用都用 screen 模式，一行解决，不用封装层

```bash
# 本地Codex
screen -dmS codex-task bash -c "codex exec --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox '任务' 2>&1 | tee /tmp/codex.log"

# 本地Claude
screen -dmS claude-task bash -c "claude --print '任务' 2>&1 | tee /tmp/claude.log"

# 韩国Codex
ssh -i ~/.ssh/id_ed25519 root@43.108.18.71 "su - ccuser -c 'screen -dmS kr-task bash -c \"codex exec ...\"'"

# omx
screen -dmS omx-task bash -c "omx autopilot '任务' 2>&1 | tee /tmp/omx.log"

# omc
screen -dmS omc-task bash -c "omc ralphthon '任务' 2>&1 | tee /tmp/omc.log"
```

### 结论
- **不需要 ai_coder 封装层**，直接 screen + 命令就行
- **screen 比 --no-wait 更可靠**，完全隔离，SSH断开不受影响
- **所有工具（Codex/Claude/omx/omc）都可用 screen**

### 教训
遇到问题先想最简单的解法，别一开始就搞复杂系统。今天折腾了一天，一行screen命令就解决了。

### screen vs --no-wait
- screen 比 --no-wait 更可靠
- screen完全隔离，SSH断开不受影响
- --no-wait可能被系统回收

## 教训总结
遇到问题先想最简单的解法，别一开始就搞复杂系统。今天我脑子抽了，绕了一大圈。
