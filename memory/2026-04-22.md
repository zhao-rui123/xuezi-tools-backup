
## 21:00+ 今日进展记录

### V2Ray DNS 优化
- V2Ray DNS 改成了运营商 DNS（192.168.31.1）+ 阿里 DNS（223.5.5.5），比 DoH 更稳定
- 腾讯视频 QQLive 桌面客户端有自己的硬编码 DNS，改系统 DNS 对它没用
- V2Ray 配置文件：~/.V2rayU/config.json
- 日志文件：~/.V2rayU/v2ray-core.log
- 腾讯视频卡顿原因是那部剧的 CDN 问题，不是我们的问题

### 韩国服务器优化
- 删除了 swapfile2（8G），节省8G磁盘空间
- 韩国服务器当前磁盘使用 19G/40G（49%）

### Right.codes GPT-5.4 API
- API地址：https://www.right.codes/codex/v1/responses
- API Key：[已删除]
- 模型名：gpt-5.4-high
- 已添加到 openclaw.json，provider 名：rightcodes
- **已知问题**：Responses API 会话续接时 response_id store=false 导致 404，需要新对话或新 session 使用
- 费用约 $0.004/K tokens，比 Feinian 便宜
- 快捷切换：/model rightcodes/gpt-5.4-high

### 模型列表（6个）
| Provider | 模型 |
|----------|------|
| minimax-cn | MiniMax-M2.7 |
| feinian | gpt-5.4 |
| rightcodes | gpt-5.4-high |
| bailian | kimi-k2.5 |
| kimi-coding | kimi-for-coding |
| nvidia-k2.5 | kimi-k2.5 |

### 关于续费 Opus
- 结论：日常任务 + GPT-5.4 代理已经够用，Opus 续费性价比不高
- 三层模型体系：MiniMax(日常) + GPT-5.4(开发) + Opus(架构/审核)


---

## 22:00-23:14 今晚修复与验证

### V2RayU 代理挂了 → 修复
- 症状：V2RayU 启动后代理不通，curl 测试 hang
- 排查：tail ~/.V2rayU/v2ray-core.log 发现"dial tcp 34.x.x.x:443: i/o timeout"
- 解决：重启 V2RayU（勾选"开机自启"后 reload）

### 快照系统彻底修复 ✅
- **问题根因（Codex 找到）**：①3月29日hooks被删、②4月16日cron快照job被删、③4月9日snapshot.py被移归档
- **修复**：恢复了 snapshot.py + cron 重新加回（`*/10 * * * *`）
- **验证**：今天已自动保存 2 次，`memory/2026-04-22.json` 存在 ✅
- **快照保存机制**：每天一个文件，每次覆盖，不产生多余文件
- **清理**：删除了 memory/snapshots/ 里 1628 个3月份旧快照文件（6.4MB → 24KB）
- **保留策略**：无清理逻辑，天然"只留当天最新"（一天一个文件覆盖）

### Right.codes 多轮对话问题
- **结论**：单轮能用，多轮续接容易 404 → 切换到 Feinian 作为主力 GPT-5.4
- **Feinian** 多轮对话正常，能保留上下文 ✅
- **rightcodes** 配置：baseUrl=`https://www.right.codes/codex/v1`，api=`openai-responses`，模型=`gpt-5.4-high`
- **可尝试改 api 为 `openai-completions`** 避免 response_id 续接 404（但有 system prompt 被覆盖风险，待用户确认）

### Codex 问题排查五步法（方法论）
记入 AGENTS.md：
1. **拆解问题** → 先拆解成最小可验证单元
2. **找断点** → 找引用链中失效的那个环节
3. **隔离验证** → 逐个排除
4. **小改动修复** → 只改有问题的部分
5. **验证+回滚** → 改完测试，有问题立刻回滚

### Feinian API 配置
- Provider：feinian
- API Key：sk-b ZeU2NTktNzRiMi1iOTJlLTQ2ZjktOTgxYS1mNTYwZjhiYjBiNWJb20（待更新）
- Base URL：已配置（feinian-proxy，见 MEMORY.md）
- 模型路由优先级：feinian/gpt-5.4（主力）
