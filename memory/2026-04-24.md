# 2026-04-24 Daily Memory

## 🧠 今天学到的关键教训

### Codex 方法论（从雪子那里学到的）

1. **先读手册再动手** — 不熟悉的任务先读 AGENTS.md，不凭记忆乱来
2. **最小改动原则** — 只改问题点，其他不动
3. **改动前先备份** — 万一翻车能回滚
4. **改完立刻验证** — 确认生效再收工
5. **找根因不治标** — 从源头解决，不是打补丁（今天 backup-check.sh 误报的教训：根因是 v2.4 升级后检查位置变了）
6. **汇报清晰** — 改了什么、为什么改、不改什么

### 问题排查五步法（Codex实战）
1. 找关键引用 + 断点定位（时间节点）
2. 并排对比新旧版本
3. 追踪调用链
4. 本地验证（不依赖外部）
5. 分层修复（P0立刻修，P1改配置先问雪子）

### AGENTS.md 今天修订（2026-04-24）
- 模型分工：MiniMax日常 → DeepSeek中型 → Codex复杂
- OpenClaw主用MiniMax，Claude Code用MiniMax+DeepSeek，Codex用官方GPT
- 调用规范：screen/curl/sessions_spawn 三分法
- acpx 停用，Claude Code 全部走 screen
- sessions_spawn 只用于非CLI工具类子任务
- 额度敏感行删除（Codex额度没那么敏感了）
- OMC模型路由 Opus 移除（5月Opus退役）
- 快速判定简化：CLI任务→screen，API测试→curl，工具类→sessions_spawn

### 文件分享脱敏规范
触发词：分享/发给朋友/发给你XX
清单：API Key→[YOUR_KEY]，Cookie/user_id→删，个人路径→通用路径，密码/Token→全删

---

## 📊 模型能力实测（雪子反馈）

### DeepSeek V4 Flash 测评结果
- 文本分析：比MiniMax强，接近ChatGPT5.4
- 编程：植物大战僵尸程序比MiniMax更丰富
- 结论：可以作为生产主力模型

### 模型能力排序（雪子实测）
1. GPT-5.4 (Codex) — 完整度最强
2. DeepSeek V4 Flash — 接近GPT-5.4，性价比最高
3. MiniMax M2.7 — 性价比高，但有差距

### DeepSeek V4 定价（重要！）
| | V4 Pro | V4 Flash |
|--|--------|----------|
| 输入（未命中） | 12元/M | 1元/M |
| 输出 | 24元/M | 2元/M |
| 差距 | 12倍 | - |

Pro贵+限流，主用Flash，Pro等下半年降价

---

## 🔧 系统配置今天做的事

### 清理项目（今天）
1. 删除 `cron_stock_report.json` — 引用不存在文件
2. 删除 `scripts/stock_report.py` — 旧版股票推送
3. 删除 `scripts/stock_report_v2.py` — 旧版股票推送v2
4. 归档 `ops/` 整个目录 — ops架构曾让定时任务全挂，已不用
5. 保留一份crontab备份，其余删除
6. 修复 `backup-check.sh` — 从检查旧tar.gz改为检查v2.4的manifest清单文件

### 本地Codex可用（今天发现）
- Mac mini上同时有 `claude` (Claude Code)和 `codex` (Codex CLI)
- Codex版本：codex-cli 0.124.0，自称GPT-5.5（比韩国服务器的5.4还新）
- Codex必须走代理（export https_proxy）

---

## 💰 省钱计划（雪子决定）

### 5月起停续费
- Opus — GPT-5.5编码能力超过Opus了
- Kimi — 49元/月太贵，DeepSeek V4 Flash才~18元/月且能力更强
- 保留：MiniMax（OpenClaw skill/MCP全家桶）、DeepSeek V4 Flash（Claude Code开发）、Codex GPT-5.5（复杂任务）

### 新三元组合
- OpenClaw → MiniMax（日常对话+skill/MCP）
- Claude Code CLI → DeepSeek V4 Flash（开发）
- Codex → GPT-5.5（架构+复杂任务）

---

## ✅ 今天完成的工作

1. DeepSeek V4 Flash API接入OpenClaw（测试成功）
2. AGENTS.md全面修订（冲突清除/模型更新/调用规范统一）
3. 旧配置文件大清理（4个文件删除，ops归档）
4. backup-check.sh脚本修复
5. Claude Code本地Codex CLI验证可用
6. Codex/Claude Code调用规范彻底厘清

---

## ✅ Claude Code + DeepSeek 思考模式验证成功（2026-04-25 凌晨）

### 关键配置（~/.claude/settings.json）
```json
{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-829a69f62a054d0f9a9ff3d79d7909b0",
    "ANTHROPIC_BASE_URL": "https://api.deepseek.com/anthropic",
    "ANTHROPIC_MODEL": "deepseek-v4-flash",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "deepseek-v4-flash",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "deepseek-v4-flash",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "deepseek-v4-flash",
    "ANTHROPIC_REASONING_MODEL": "deepseek-v4-flash",
    "effortLevel": "high",
    "API_TIMEOUT_MS": "3000000"
  }
}
```

### 重要发现
- 必须用 `/anthropic` 端点（Claude Code 原生兼容），不是 `/v1`
- `effortLevel: high` + `ANTHROPIC_REASONING_MODEL` 是开启思考的关键
- Claude Code 交互模式能显示 thinking 过程
- 2分钟任务产出 ~10000 tokens，质量远超普通模式

### 雪子的 ccswitch 配置参考
雪子的笔记Air配置用 `deepseek-v4-flash` 作为所有默认模型（haiku/sonnet/opus同一化）
