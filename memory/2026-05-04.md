## 2026-05-04 durable memory

- 对 `agents/energy_solution_agent/energy_solution_agent/` 做了大规模增强，目标从“专业级工具”推进到“行业大师级新能源方案/前置可研 agent”。
- 已完成并推送多轮关键改造，核心方向包括：
  - 修复储能 sizing 逻辑：`estimate_storage` 不再直接取最后一个候选，而是按经济性择优。
  - 增加 `scripts/econ_scan.py`，可做 PV×储能组合的经济扫描，按 IRR / NPV 排序。
  - 风电容量支持原生 `wind_mw` override，不再依赖 engine.py 临时 hack。
  - 修复多项真实 bug：例如 `positive[0]` 未排序、储能碳减排低估、海外项目税法显式控制、融资逻辑重复覆盖等。
  - 新增分项成本指标：`pv_lcoe`、`wind_lcoe`、`storage_lcos`、系统级 LCOE/LCOS 展示增强。
  - 收益拆解从标签升级为金额化 breakdown。
  - 增加基线年能源成本、自行消纳率、储能循环预警等结果项。
  - 升级时序体系：支持工作日/周末双日型、季节性日型、168 小时周型扩年。
  - 升级敏感性分析：从简单比例缩放升级为真实场景重跑（电价、CAPEX、发电量等扰动场景）。
  - 升级资源输入：支持精确 8760 光伏/风电输入，避免把全年序列再乘 365 的错误。
  - 新增自动资源抓取 `resource_fetch.py`：第一版接入 NASA POWER（光照）和 Open-Meteo（风速/温度）。
  - 自动资源建模升级：NASA/Open-Meteo 抓到的气象数据可自动转成 PV / 风电功率序列。
  - 新增设备模型库 `device_models.py`：包含光伏组件模型、风机模型、hub-height 风速换算、NOCT 温度模型等。
  - 报告交付升级：新增 `report_charts.py`，自动生成 4 张标准图表并插入 docx，含中文字体适配。
- 新增模板与交付体系：
  - `templates/master_input_template.json`
  - `templates/master_delivery_checklist.md`
  - `templates/device_model_catalog.md`
  - README 已更新，沉淀为“大师级标准模板包”。
- 关键最终判断：这套 agent 已经从“可跑的分析工具”升级为“行业大师级新能源方案 / 前置可研 agent”，且具备自动抓资源、自动建模、自动优化、自动报告、自动插图的完整链路。
- 重要交付偏好：后续如果再增强，优先考虑补《使用说明 + 最佳实践手册》，而不是继续无边界堆功能。
