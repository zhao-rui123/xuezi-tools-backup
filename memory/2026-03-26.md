# 2026-03-26 记忆记录

*自动生成于 2026-03-26 00:00:00*

## 今日概览

- 早上与雪子完成模型切换

## 会话记录

### 模型切换 (07:27-07:34)

**决策**:
- 雪子要求将默认模型从 bailian/kimi-k2.5 改为 minimax-cn/MiniMax-M2.7
- 流程：先 git commit 备份 → 设置30分钟回滚 → 修改配置 → 测试成功 → 取消回滚
- Kimi Coding API Key 测试结果：两个 Key (`sk-kimi-g6iZP...` 和 `sk-kimi-vmWH...`) 全部返回 Invalid Authentication，**已失效**

**最终配置**:
- 默认模型：`minimax-cn/MiniMax-M2.7`
- Kimi 模型：**不可用**（API Key 全部失效）

**问题**:
- kimi-coding 和 kimi-k2.5 模型因 API Key 失效暂时无法使用
- 雪子未决定是否重新配置 Kimi Key

### 模型配置完成 + Kimi Key更新 (07:35-07:45)
- 新Key `sk-kimi-AMBdWAslYV...` 有效，已添加到 `kimi-coding` provider
- **重要配置要点**：Kimi Coding 必须用 `https://api.kimi.com/coding` 端点（非标准moonshot API）
- 必须写在 provider 的 `apiKey` 字段，不能用环境变量 `KIMI_API_KEY`
- 默认模型：MiniMax-M2.7
- bailian 模型少用，kimi-coding 可用于代码任务

### Tailscale远程访问计划 (07:58-08:10)
- 计划：Mac mini + 笔记本组网，实现远程调度
- 免费版足够（2人用）
- Mac mini 上未安装 Tailscale，需今晚回家后配置
- 笔记本是 Mac，连接不同 WiFi 也能用

### CALB储能收资清单密码保护 (08:45)
- 服务器：106.54.25.161:5002
- `/submissions` 页面添加密码保护
- 密码：zr041412150
- 访问方式：`http://106.54.25.161:5002/submissions?pwd=zr041412150`

### 股票推送修复 + SSH Key配置 (16:50-17:42)

**修复内容**:
1. MACD/KDJ失败原因：`sys.path.insert(0, "skills/stock-suite")` 相对路径错误 → 改为绝对路径
2. 换手率/市盈率/市净率等为空：字段名日期格式缺少花括号 `{2026-03-26}` → 修复为 `f'{{{today}}}'`
3. 新浪K线API正常，无需换API

**简化通知**:
- 16:30自选股推送改为精简版，只显示：股票名、价格、涨跌幅、MACD、KDJ
- 去除详细数据（换手率、量比、市盈率、市净率、成交额）

**SSH Key配置**:
- Mac mini：生成了 Ed25519 私钥 `~/.ssh/id_ed25519`，公钥已添加到服务器
- 雪子笔记本（Windows）：生成了 Ed25519 私钥，公钥已添加到服务器
- TOOLS.md 已更新，记录两台机器的SSH连接命令

**自选股更新**:
- 新列表：比亚迪(002594)、比亚迪港股(08174.HK)、赣锋锂业(002460)、盛新锂能(002240)、京东方A(000725)、国城矿业(000688)
- MEMORY.md 和 USER.md 已同步更新
